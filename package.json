{
  "name": "script-studio",
  "version": "0.1.0",
  "private": true,
  "description": "Local app that turns a video title + source links into an approved, style-checked P.O.P voice-over script",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "npm run start -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
